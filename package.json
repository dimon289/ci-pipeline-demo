{
  "name": "ci-pipeline-demo",
  "version": "1.0.0",
"scripts": {
  "build": "npm test",
  "test": "jest",
  "lint": "eslint ."
},

  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}
